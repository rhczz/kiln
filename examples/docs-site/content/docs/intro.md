---
title: "Introduction"
description: "What this documentation site demonstrates."
---

This example uses a custom `docs` collection with the route `/docs/{slug}/`.

## Structure

Each guide is a Markdown file under `content/docs`.

## Templates

The collection uses `templates/doc.html`.
